@model IEnumerable<Horeca.DataBaseLibrary.Models.CustomModels.LocationToDepartmentModel>
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
<div class="row">
    <div class="col-12 col-xl-12">
        <div class="card">
            <div class="card-header">
                <h5 class="card-title">Table of departments</h5>

            </div>
            <table class="table">
                <thead>
                    <tr>
                        <th style="width:40%;">Name</th>
                        <th style="width:25%">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @item.Department
                            </td>
                            <td>
                                <a class="btn btn-pill btn-primary btnAdd" asp-controller="Product" asp-action="CreateProductFromDepartment" asp-route-id="@item.Id">Add Product</a>
                                <a class="btn btn-pill btn-primary btnTableWith" asp-controller="Product" asp-action="ViewProductByIdDepartment" asp-route-id="@item.Id">View Product</a>
                                <a class="btn btn-pill btn-danger" asp-controller="Department" asp-action="EditDepartment" asp-route-id="@item.Id">Edit</a>
                                <a class="btn btn-pill btn-danger" asp-controller="Department" asp-action="DeleteDepartment" asp-route-id="@item.Id">Delete</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
<div>
    <a class="btn btn-pill btn-primary btnAdd" asp-controller="Department" asp-action="CreateDepartment">Add</a>
</div>